import { FinalBorrowedBook } from './final-borrowed-book';

describe('FinalBorrowedBook', () => {
  it('should create an instance', () => {
    expect(new FinalBorrowedBook()).toBeTruthy();
  });
});
